#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

### if [[ "$(tty)" = "/dev/tty1" ]]; then
###     pgreg dwm || startx
### fi

export QT_QPA_PLATFORMTHEME="qt5ct" 
alias grep='grep --color=auto'
